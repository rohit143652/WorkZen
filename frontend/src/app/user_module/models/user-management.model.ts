import { EmployeeResponse } from '../../employee_module/models/employee.model';

/** User Management is a view over employee-linked login accounts. */
export type UserManagementRow = EmployeeResponse;
